# При создании необходимо указывать полную версию образа (желательно алпайн так как она легковесная)
FROM node:21.7.2-alpine3.18

# Создаем рабочую директорию
WORKDIR /app

# Копируем package.json и yarn.lock в рабочую директорию
COPY package*.json ./

# Устанавливаем зависимости
RUN npm install

# Копируем остальные файлы проекта в рабочую директорию
COPY . .

# Собираем приложение для production
RUN npm run build

# Устанавливаем сервер для обслуживания статических файлов
RUN npm install -g serve

# Определяем команду запуска контейнера
CMD ["serve", "-s", "dist"]

# Открываем порт, на котором будет доступно наше приложение
EXPOSE 3000